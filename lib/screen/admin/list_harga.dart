import 'package:flutter/material.dart';

class ListHargaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List Harga'),
      ),
      body: Center(
        child: Text('Halaman List Harga'),
      ),
    );
  }
}
